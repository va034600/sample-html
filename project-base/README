前提
・ruby
・npm
・npm install -g grunt-cli
・npm install -g bower


起動手順
・プロジェクトのroot移動
・npm install
・grunt (各種コマンド)

gruntコマンド
・grunt watch
	watchする
		対象ファイルが更新されたら、以下を実施する
			・cssを作成する。
			・liveReloadを実行する。

・grunt connect
	ローカルにサーバを立ち上げる。
	http://localhost:8000

・grunt
	watchとconnectを行う。

・grunt bower
	javascriptをダウンロードする。

・grunt requirejs
	jsをrequire_config.jsをもとに結合する。
	出力先はjs/main.min.jsに出力される。

・grunt compass
	sassファイルをコンパイルします。

・grunt mincss
	指定のcssを圧縮する

・grunt mincompass
	compassとmincompassを実行する。

・grunt jshint
	jsを静的チェックしてくれる。


connect
LiveReload
sass compass
r.js
grunt.js
Bower
requirejs
backbone.js
	router
	view
	text
	model
	i18n
foundation